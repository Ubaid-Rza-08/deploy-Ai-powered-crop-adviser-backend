apiVersion: v1
kind: Service
metadata:
  name: ai-service
  namespace: gemini-crop-adviser
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: "elb"
spec:
  type: LoadBalancer
  selector:
    app: ai-service
  ports:
    - protocol: TCP
      port: 8082
      targetPort: 8082